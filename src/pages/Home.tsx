import {ArrowRight,Landmark,Percent,ReceiptText,ShieldCheck,Search} from "lucide-react";import {Link} from "react-router-dom";import {useMemo,useState} from "react";import {useBanks} from "../hooks/useBanks";import BankBadge from "../components/BankBadge";import Score from "../components/Score";export default function Home(){const{banks,loading,demo}=useBanks();const[q,setQ]=useState("");const shown=useMemo(()=>banks.filter(b=>b.name.toLowerCase().includes(q.toLowerCase())),[banks,q]);const rankedByScore=[...banks].filter(bank=>bank.healthScore>0),rankedBySavings=[...banks].filter(bank=>bank.products.savingsRate!=null),rankedByLoan=[...banks].filter(bank=>bank.products.loanRate!=null),rankedByFee=[...banks].filter(bank=>bank.products.transferFee!=null);const top=rankedByScore.sort((a,b)=>b.healthScore-a.healthScore)[0],save=rankedBySavings.sort((a,b)=>(b.products.savingsRate??-1)-(a.products.savingsRate??-1))[0],loan=rankedByLoan.sort((a,b)=>(a.products.loanRate??Number.POSITIVE_INFINITY)-(b.products.loanRate??Number.POSITIVE_INFINITY))[0],fee=rankedByFee.sort((a,b)=>(a.products.transferFee??Number.POSITIVE_INFINITY)-(b.products.transferFee??Number.POSITIVE_INFINITY))[0];const formatRate=(value:number|null|undefined)=>value==null?'Pending':`${value}%`;const formatMoney=(value:number|null|undefined)=>value==null?'Pending':`GH₵${value.toFixed(2)}`;return <>
<section className="hero shell"><div><span className="eyebrow">Independent bank comparison</span><h1>See the bank<br/>behind the brand.</h1><p>Compare financial strength, savings rates, loan costs and everyday fees in one clear view.</p><div className="search"><Search size={20}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search a bank"/><a href="#rankings" className="button">View results</a></div><small className="muted">Public data, clearly sourced. Rates and fees may change.</small></div><div className="hero-card"><div className="between"><div><small>This month's snapshot</small><h2>Ghana banking overview</h2></div>{demo&&<span className="pill">Demo data</span>}</div><div className="snapshot"><div><b>{banks.length||4}</b><span>Banks tracked</span></div><div><b>{top?.healthScore??'--'}/100</b><span>Top health score</span></div><div><b>{formatRate(save?.products.savingsRate)}</b><span>Top listed savings</span></div><div><b>{formatMoney(fee?.products.transferFee)}</b><span>Lowest transfer fee</span></div></div><p className="hero-note">Start with your goal. BankLens brings the most relevant numbers to the top.</p></div></section>
<section className="shell section"><div className="section-head"><div><span className="kicker">Quick answers</span><h2>Choose what matters to you</h2></div></div><div className="answer-grid"><Answer icon={<ShieldCheck/>} label="Strongest overall" bank={top?.name} value={top?`${top.healthScore}/100`:'Pending'}/><Answer icon={<Percent/>} label="Best listed savings" bank={save?.name} value={formatRate(save?.products.savingsRate)}/><Answer icon={<Landmark/>} label="Lowest listed loan rate" bank={loan?.name} value={formatRate(loan?.products.loanRate)}/><Answer icon={<ReceiptText/>} label="Lowest transfer fee" bank={fee?.name} value={formatMoney(fee?.products.transferFee)}/></div></section>
<section id="rankings" className="shell section"><div className="panel"><div className="section-head"><div><span className="kicker">Bank rankings</span><h2>Compare at a glance</h2></div><Link to="/compare">Full comparison <ArrowRight size={16}/></Link></div>{loading?<div className="loading">Loading bank data...</div>:<div className="table-wrap"><table><thead><tr><th>Bank</th><th>Health</th><th>Savings</th><th>Loan</th><th>Transfer</th><th>Assets</th><th></th></tr></thead><tbody>{shown.map(b=><tr key={b.id}><td><BankBadge bank={b}/></td><td><Score value={b.healthScore}/></td><td>{formatRate(b.products.savingsRate)}</td><td>{formatRate(b.products.loanRate)}</td><td>{formatMoney(b.products.transferFee)}</td><td>{b.metrics.assets==null?'Pending':`GH₵${b.metrics.assets}bn`}</td><td><Link to={`/banks/${b.slug}`}>View</Link></td></tr>)}</tbody></table></div>}</div></section>
<section className="method-strip"><div className="shell steps"><Step n="1" title="We collect" text="Published statements, product rates and fee schedules."/><Step n="2" title="We verify" text="Every value keeps its source, period and review status."/><Step n="3" title="You compare" text="Clear filters help you decide based on your own priorities."/></div></section></>}
function Answer({icon,label,bank,value}:{icon:React.ReactNode;label:string;bank?:string;value:string}){return <article className="answer">{icon}<h3>{label}</h3><p>{bank||'Pending'}</p><b>{value}</b></article>}function Step({n,title,text}:{n:string;title:string;text:string}){return <article><span>{n}</span><h3>{title}</h3><p>{text}</p></article>}