export default function Score({value}:{value:number|null}){if(value==null||value<=0)return <span className="score" title="BankLens health score is pending review">Pending<small>Review</small></span>;const label=value>=85?'Strong':value>=75?'Good':'Watch';return <span className="score" title={`BankLens health score: ${value} out of 100`}>{value}<small>{label}</small></span>}