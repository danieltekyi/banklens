import {Link,NavLink,Outlet} from "react-router-dom";import {Menu,X} from "lucide-react";import {useState} from "react";const links=[['/','Overview'],['/compare','Compare'],['/products','Products'],['/methodology','Methodology'],['/admin','Admin']];export default function Layout(){const[open,setOpen]=useState(false);return <div className="app"><header className="topbar"><div className="shell nav"><Link className="brand" to="/"><span className="brandmark">BL</span><span>BankLens<small>Ghana</small></span></Link><nav className="desktop-nav">{links.map(([to,label])=><NavLink key={to} to={to}>{label}</NavLink>)}</nav><Link className="button small desktop-cta" to="/compare">Compare banks</Link><button className="menu" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="mobile-nav">{links.map(([to,label])=><NavLink onClick={()=>setOpen(false)} key={to} to={to}>{label}</NavLink>)}</nav>}</header><main><Outlet/></main><footer><div className="shell foot"><div><b>BankLens</b> by Tiwaak</div><p>Information only, not financial advice. Confirm current terms with each bank.</p></div></footer></div>}