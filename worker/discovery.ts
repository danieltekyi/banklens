const abs=(href:string,base:string)=>{try{return new URL(href,base).href}catch{return null}};const clean=(s:string)=>s.replace(/<[^>]+>/g," ").replace(/&amp;/g,"&").replace(/\s+/g," ").trim();export async function discoverCountry(env:Env,countryId:number){const country=await env.DB.prepare("SELECT * FROM countries WHERE id=? AND enabled=1").bind(countryId).first<any>();if(!country)throw new Error("Country is not enabled");const res=await fetch(country.bank_directory_url,{headers:{"User-Agent":"BankLensBot/0.2 (+https://banklens.tiwaak.com/methodology)"}});if(!res.ok)throw new Error(`Regulator returned ${res.status}`);const html=await res.text();const links=[...html.matchAll(/<a[^>]+href=["']([^"']+)["'][^>]*>([\s\S]*?)<\/a>/gi)].map(m=>({url:abs(m[1],country.bank_directory_url),title:clean(m[2])})).filter(x=>x.url&&x.title);let found=0;for(const link of links){const u=new URL(link.url!);if(!/^https?:$/.test(u.protocol))continue;const kind=/annual|report|financial|statement|investor/i.test(link.title+link.url)?"financial":/fee|tariff|rate|loan|saving|product/i.test(link.title+link.url)?"product":"candidate";await env.DB.prepare("INSERT OR IGNORE INTO discovered_links(country_id,url,kind,title) VALUES(?,?,?,?)").bind(countryId,link.url,kind,link.title.slice(0,300)).run();found++}await env.DB.prepare("UPDATE countries SET discovery_status='completed',last_discovered_at=? WHERE id=?").bind(new Date().toISOString(),countryId).run();return{country:country.name,linksFound:found}}export async function runDiscovery(env:Env){const{results}=await env.DB.prepare("SELECT id FROM countries WHERE enabled=1").all<{id:number}>();const output=[];for(const c of results){try{output.push(await discoverCountry(env,c.id))}catch(error){output.push({countryId:c.id,error:String(error)})}}return output}