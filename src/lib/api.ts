import type {ApiResponse,Bank} from "../types"; export async function getBanks():Promise<ApiResponse<Bank[]>>{const r=await fetch("/api/banks");if(!r.ok)throw new Error("Could not load bank data");return r.json()} export async function getBank(slug:string):Promise<ApiResponse<Bank>>{const r=await fetch(`/api/banks/${slug}`);if(!r.ok)throw new Error("Bank not found");return r.json()}