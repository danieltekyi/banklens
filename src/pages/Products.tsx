import {useBanks} from "../hooks/useBanks";import BankBadge from "../components/BankBadge";export default function Products(){const{banks}=useBanks();const formatRate=(value:number|null)=>value==null?'Pending':`${value}%`;const formatMoney=(value:number|null)=>value==null?'Pending':`GH₵${value.toFixed(2)}`;const formatBalance=(value:number|null)=>value==null?'Pending':`GH₵${value}`;return <section className="shell page"><span className="kicker">Everyday banking</span><h1>Products and costs</h1><p className="lead">A simple starting point for savings, borrowing and transfer costs.</p><div className="card-grid">{banks.map(b=><article className="product-card"><BankBadge bank={b}/><div className="product-row"><span>Savings rate</span><b>{formatRate(b.products.savingsRate)}</b></div><div className="product-row"><span>Listed loan rate</span><b>{formatRate(b.products.loanRate)}</b></div><div className="product-row"><span>Transfer fee</span><b>{formatMoney(b.products.transferFee)}</b></div><div className="product-row"><span>Minimum balance</span><b>{formatBalance(b.products.minimumBalance)}</b></div><p className="source-note">Demo terms. Confirm current eligibility and total cost with the bank.</p></article>)}</div></section>}